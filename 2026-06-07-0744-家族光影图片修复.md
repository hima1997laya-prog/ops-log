# 家族光影图片修复

## 操作意图
修复 `qiuqiu-grandpa-bond` 网站 `/family-memories` 页面图片显示与文字内容不匹配的问题。

## 上下文
- 6月6日用户按#1~#13顺序重新上传了13张童年老照片（pic-01.jpg ~ pic-13.jpg）
- commit 63678e2 将图片重命名为 `photo-xx-描述.jpg`，但 `page.tsx` 中 stories 数组缺失 #6（龙龙舅舅不高兴夏天），且顺序被打乱
- 用户反馈：图片显示不对，"按原来1#到13#顺序对应着文字内容"

## 执行计划
1. 确认图片文件内容映射关系（pic-xx → photo-xx-描述.jpg 的内容未变）
2. 在 `page.tsx` 的 stories 数组中补充 #6 不高兴夏天
3. 验证全部13条记录按 #1~#13 顺序排列
4. 提交并推送到 `hima1997laya-prog/qiuqiu-grandpa-bond` main 分支
5. Cloudflare Pages 自动构建部署

## 执行结果
- commit d37766f 已推送
- 用户确认："图片显示成功" ✅

## 后续计划
- 所有操作先在 ops-log 记录，讨论确认后再执行
