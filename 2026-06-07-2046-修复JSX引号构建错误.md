# 修复JSX引号构建错误（导致页面无音效/无声）

## 问题
用户反馈滑动页面后没有任何声音。

## 排查过程
1. ✅ 代码逻辑正确
2. ✅ 音频文件存在、音量已增益到正常水平（-10.6dB）
3. ❌ **本地 `npm run build` 发现构建错误！**

## 根因
commit 中追加 #6 不高兴夏天文本时使用了半角双引号 `"`：
`"却被一句"来，看镜头，笑一个"硬生生打断"`

JSX 解析器将字符串中的半角双引号 `"` 识别为字符串结束符，导致解析失败。
→ **Cloudflare Pages 所有新代码的构建全部失败！**
→ 用户看到的始终是旧版页面（无音频功能），所以怎么操作都没声音

## 修复内容
1. 半角双引号 → 全角书名号「」（JSX安全）
2. 音频触发方式从 `scroll` + `touchmove` 扩展到 `scroll` + `touchmove` + `wheel` + `click`
3. `.catch(() => {})` → `.catch((err) => { console.error(...) })` 不再静默吞错误
4. 移除 `{ once: true }`，改为在 `startAudio` 内部统一 removeEventListener
5. 本地 `npm run build` 验证通过

## 提交
- commit 407b339 → hima1997laya-prog/qiuqiu-grandpa-bond main
- 等待 GitHub Actions 构建 + Cloudflare Pages 部署
