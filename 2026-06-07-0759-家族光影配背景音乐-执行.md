# 家族光影配背景音乐 — 执行

## 操作意图
为 `/family-memories` 页面添加英文背景音乐：点击图片区域时自动播放温暖钢琴曲，循环播放。

## 已确认方案
- 龙姥爷定：方案A（点击图片→底部出现音乐播放条，图片不动）+ 循环播放 ✅
- 曲目：**"Warm Memories - Emotional Inspiring Piano"** by Keys of Moon Music
- 协议：CC BY 4.0（需署名）

## 执行计划
1. 将 `warm-memories.mp3` 放入 `qiuqiu-grandpa-bond/public/audio/`
2. 修改 `page.tsx`，在每个图片区域添加 `onClick` 事件：
   - 点击图片 → 创建/显示 `<audio>` 播放器
   - 浮动控制条在页面底部（播放/暂停/音量/关闭）
   - 循环播放
3. 页面底部加 CC BY 署名
4. 提交推送

## 执行结果
（待执行后填写）
