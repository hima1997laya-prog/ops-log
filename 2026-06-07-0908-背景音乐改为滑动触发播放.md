# 背景音乐改为滑动触发播放

## 操作意图
将音乐触发方式改为：用户滑动（滚动）页面时自动播放音乐，循环播放。

## 上下文
- 用户要求：打开页面就播音乐，但浏览器禁止 autoplay
- 之前提了"点击进入遮罩层"方案
- 用户新方案：**滑动就播放** — 用户滚轮下滑/触摸滑动页面时触发音乐

## 技术方案
- 监听 `scroll` 事件（或 `wheel` / 触摸滑动）
- 用户首次滑动时 → 创建 Audio 实例并 play() → 由于 scroll 是用户交互，浏览器允许播放
- 播放后移除 scroll 监听，避免重复触发
- 音乐循环播放，底部控制条保留

## 执行计划
1. 修改 page.tsx：移除 image onClick，改为首次 scroll 触发
2. 保留底部播放控制条
3. 保留 CC BY 署名
4. 提交推送

## 状态
待执行（用户已确认此方案）
